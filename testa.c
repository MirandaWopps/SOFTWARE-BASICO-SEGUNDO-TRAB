#include <stdio.h>
#include "cria_func.h"

int main(void) {
	// Primeiro parametro: endereco
	// colocAq
	// Segundo: descricao dos parametros

	// Terceiro: qtd parametros

	// Quarto: vetor com o codigos


	//cria_func(void* f, DescParam params[], int n, unsigned char codigo[]);
	unsigned char codigo[32];
	printf("%d, size of void %d \n", sizeof(codigo), sizeof(int) );

	return 0;
}
