#include "cria_func.h"

/** Cria_func 
* 
* 
* @authors Eu
* @param f tem endereço da func original
* @param params descricao dos params para chamar esa func. Variam entre PARAM(o parametro nao e amarrado, ou seja, deve ser recebido pela nova func e repassado a func orgin), FIX(o parametro deve ser amarrado a um valor constante fornecido no campo valo) e IND(o parametro deve ser amarrado a uma variavel cujo endereco esta no campo valor, ou seja, passado a func original o valor corrente dessa var.   
* @param n numero de parametros descritos por params e codigo
* @param codigo vetor onde code gerado sera gravado
* @return nothing.
*/
void cria_func (void* f, DescParam params[], int n, unsigned char codigo[]){
  
  
  
}
